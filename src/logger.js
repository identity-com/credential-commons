module.exports = {
  // eslint-disable-next-line no-unused-vars
  warn(a, b = undefined) {
    // eslint-disable-next-line no-console
    console.warn(a);
  },
  // eslint-disable-next-line no-unused-vars
  info(a, b = undefined) {
    // eslint-disable-next-line no-console
    console.info(a);
  },
  // eslint-disable-next-line no-unused-vars
  debug(a, b = undefined) {
    // eslint-disable-next-line no-console
    // console.debug(a); // disabled
  },
  // eslint-disable-next-line no-unused-vars
  error(a, b = undefined) {
    // eslint-disable-next-line no-console
    console.error(a);
  },
};
